discord.py>=2.3.0
python-dotenv>=1.0.0
pre-commit>=4.0.0
sqlmodel>=0.0.22
asyncpg>=0.29.0
motor>=3.7.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0
mongomock-motor>=0.0.21
# Ships the IANA time zone database for zoneinfo. python:*-slim is Debian slim,
# which does not include /usr/share/zoneinfo, so without this ZoneInfo raises at
# runtime and the weekly recap loop dies silently.
tzdata>=2024.1
